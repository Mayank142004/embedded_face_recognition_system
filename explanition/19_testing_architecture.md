# 19. Testing Architecture

> `UNKNOWN / NOT CONFIRMED FROM CODE`

The current repository does **not** contain any automated testing architecture. 
There are no:
- Unit tests (`pytest`, `unittest`)
- Integration tests
- API tests
- ML model tests or evaluation scripts in the main execution path.

The script relies entirely on manual verification (e.g., viewing `result_datas/testig_video_result.mp4` to see if the boxes were drawn correctly and checking the `marked_attendance` folder for CSVs).
